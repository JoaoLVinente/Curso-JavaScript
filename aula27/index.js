/*
Operacao ternaria
? -> para verdadeiro
: -> para falso
*/

const pontuacaoUsuario = 99;
const nivelUsuario = pontuacaoUsuario >= 1000 ? 'Usuario VIP' : 'Usuario normal';

console.log(nivelUsuario);

//Isso pode ser usado para substituir um if e else pequeno